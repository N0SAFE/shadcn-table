// Example: Using FloatingBar
import { FloatingBarAction } from '../src/components/floating-bar';

export default function FloatingBarExample() {
  // You would normally provide actions and table instance
  const actions: FloatingBarAction<any>[] = [];
  const table = {} as any;
  return <div>FloatingBar would be rendered here with actions and table</div>;
}
